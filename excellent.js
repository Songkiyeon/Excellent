function excellent() {
	var a;
	var b;
	var c;
	var asd;
	var fl = [[ ], [ ]];
	var f;
	var str;
	var ans;
	var e = [[3, 2], [4, 5]];
	var d = 3;
	var num = 3;
	var flag = true;
	asd = 'c';
	c = e[1][2];

	if (flag && flag) {
		c = 2 + a + b;
	}

	if (typeof(a) == typeof(1)) {
		c = 2 + a + b;
	}

	if (true) {
		d = 1;
	}

	if (a === 1) {
		a = a + 1;
	}
	else if (a != 2) {
		a = 1;
	}

	while(true){
		if (b > 2) {
		b = b - 1;
		}
		else break;
	}

	while(true){
		if (b > 3) {
		b = b - 1;
		}
		else if (b > 10) {
		b = b - 10 * 10 + b * 4 / 2 + 1 * 3;
		}
		else break;
	}
	alert("A" + a + " " + b);
	str = prompt("I");
	str = str.split(" ");
	str = str[0];
	a = prompt("I");
	a = a.split(" ");
	a = a[0];
	b = a[1];
	c = a[2];
	asd = a[3];
	a = a[4];

	function plus(a, b, g) {
		ans = a + b;
		ans = a + b + c + d;
		return 1;
	}

	function root(a, b, c) {
		ans = a + 1;
		ans = a + 1;
		ans = a + 5;
		return 1;
	}
}
